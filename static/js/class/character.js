class Character {
	// id: string
	// name: string
	// icon: string (path)
	// dist: int
	constructor(id, name, icon, dist) {
		this.id = id;
		this.name = name;
		this.icon = icon;
		this.dist = dist;
		this.iconId = null;
	}
}